plugins {
    id 'java-library'
}

// The physical common sources are compiled by the NeoForge module's Minecraft-aware
// source set for now. This keeps common free of loader APIs while avoiding a separate
// Minecraft classpath model during the first split.
sourceSets {
    main {
        java.setSrcDirs([])
        resources.setSrcDirs([])
    }
}
